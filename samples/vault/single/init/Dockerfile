ARG UBUNTU_IMAGE_NAME
ARG UBUNTU_VERSION

FROM ${UBUNTU_IMAGE_NAME}:${UBUNTU_VERSION}

ARG VAULT_VERSION

WORKDIR /tmp/vault/init/

ADD ./build.sh ./
RUN sh ./build.sh ${VAULT_VERSION}

ADD ./ ./

ENTRYPOINT [ "sh", "./run.sh" ]
